# 오늘 할일 - 2026년 4월 30일

> **본 문서는 fork(Alpha-ChangukChoi/rhwp) 전용 작업 기록**입니다. 본가(edwardkim/rhwp) 의 일일 할일과는 별도 흐름.

## agent-v0.1 — rhwp 에이전트 MVP

OpenAI 기반 멀티턴 에이전트로 HWP/HWPX 문서를 웹에서 편집하는 기능의 MVP. self-hosted (Docker compose), NestJS 백엔드. 본가 절차(하이퍼-워터폴) 그대로 적용 + fork 절차 다듬기 누적([methodology_refinements.md](../manual/methodology_refinements.md)).

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| [#1](https://github.com/Alpha-ChangukChoi/rhwp/issues/1) | NestJS agent-server 스켈레톤 + docker-compose 통합 | 완료 | Stage 1 (스캐폴드+`/health` e2e 통과) → Stage 2 (multi-stage Dockerfile, USER node, 255MB) → Stage 3 (compose 통합 + rhwp-studio 동거 검증). 본가 코드 무수정 정책 유지. Deviation 4건 모두 보고서 §4 기록 (Node 22 / NestJS 11 / 이미지 255MB / restart 정책 부분 검증). 산출물: `rhwp-agent-server/` 신규 + `docker-compose.yml`/`gitignore` 수정. **방법론 평가**: R-001 (이슈 등록 미니사이클) 첫 적용 + R-007/R-008/R-009 신규 정식화. 최종 보고서 [task_agent-v0.1_1_report.md](../report/task_agent-v0.1_1_report.md) |
| [#2](https://github.com/Alpha-ChangukChoi/rhwp/issues/2) | OpenAI Chat Completions 클라이언트 + 환경변수 검증 | 완료 | Stage 1 (의존성 + ConfigModule + joi 검증, 부팅 실패 e2e) → Stage 2 (ChatService + 모킹 e2e, 단위 3 + e2e 1) → Stage 3 (옵셔널 실 API + compose 통합 검증). 본가 코드 무수정. R-007/R-008/R-009 가설 3건 모두 입증 (manual curl 0회, 도구 deviation 0건, 응답시간 자동 expect). Deviation: jest manual mock dup (src/+test/) — 후속 R-012 별도 정리 후보. **방법론 평가**: R-010~R-013 신규 정식화 (외부 정보 조회 / 누적 환경 정합성 / mock 단일 원천 / 2단계 검증 사다리). 최종 보고서 [task_agent-v0.1_2_report.md](../report/task_agent-v0.1_2_report.md) |
| [#3](https://github.com/Alpha-ChangukChoi/rhwp/issues/3) | hwpctl Action ↔ OpenAI tool 매핑 + 도구 호출 루프 | 완료 | Stage 1 (zod 4개 + ToolExecutor + StubToolExecutor, 단위 12) → Stage 2 (completeWithTools 루프 + max iter + tool 에러 message, 단위 3 + e2e 1) → Stage 3 (compose 부팅 + ChatModule init 검증). 본가 코드 무수정. **R-007~R-013 모두 사전 적용 → 3 stage deviation 0건** (#1: 6건, #2: 3건, #3: 0건의 명확한 감소 추세). R-3-D 결정 변경 사례 (zod-to-json-schema → zod v4 built-in toJSONSchema)로 *추천 검증 사이클*의 효과 검증. **방법론 평가**: R-014 (이슈 등록 시 다듬기 점검표 의무화) + R-015 (반대 입장 근거 명시) 신규 정식화. 최종 보고서 [task_agent-v0.1_3_report.md](../report/task_agent-v0.1_3_report.md) |

## 다음 후보 (agent-v0.1 마일스톤 후속 이슈)

| 후보 | 내용 |
|------|------|
| #4 (예정) | 멀티턴 세션 히스토리 관리 (in-memory, sessionId 기반) |
| #5 (예정) | rhwp-studio 우측 사이드바 채팅 UI (DOM 컴포넌트, 입력·기록 표시·도구 호출 결과 시각화) |
| #6 (예정) | 백엔드 ↔ 프런트엔드 메시지·tool 호출 프로토콜 (WebSocket 또는 SSE) |

## 별도 정리 후보 (방법론 다듬기 후속)

| 후보 | 내용 |
|------|------|
| (R-012) | jest manual mock dup 정리 (옵션 A/B/C 중 채택) |

## 방법론 평가 (오늘 누적)

본 fork 의 첫·둘째 task 완료. 본가 절차의 공백 9건 발견·정리 ([methodology_refinements.md](../manual/methodology_refinements.md)):

**task #1 (#1 이슈) 회고**
- R-001 이슈 등록 미니사이클 (적용 2회 — 효과 입증)
- R-002 Fork 셋업 사전 단계 (placeholder, 차기 fork 시 정식화)
- R-007 도구 버전은 *고정*이 아닌 *제약*으로 (신규 정식화)
- R-008 자동 검증 가능 항목은 자동화 우선 (신규 정식화)
- R-009 환경 의존적/수치형 제약은 기준치 + 허용 오차 (신규 정식화)

**task #2 (#2 이슈) 회고**
- R-007/R-008/R-009 첫 사전 적용 → 모두 가설 입증 완료
- R-010 외부 정보 조회 단계의 명시화 (신규 정식화)
- R-011 누적 환경 정합성 점검 (신규 정식화)
- R-012 jest manual mock 의 단일 진실 원천 (신규 정식화)
- R-013 2단계 검증 사다리 (jest + 컨테이너) (신규 정식화)

**task #3 (#3 이슈) 회고**
- R-007~R-013 사전 적용 → 3 stage deviation 0건 (복리 효과 입증)
- R-3-D 결정 변경 사례 (zod-to-json-schema → zod v4 built-in) — 추천 검증 사이클 효과
- R-014 이슈 등록 시 다듬기 점검표 의무화 (신규 정식화)
- R-015 수행계획서 §6 결정사항에 반대 입장 근거 명시 (신규 정식화)
