# 오늘 할일 - 2026년 5월 1일

> **본 문서는 fork(Alpha-ChangukChoi/rhwp) 전용 작업 기록**입니다. 본가(edwardkim/rhwp) 의 일일 할일과는 별도 흐름.

## agent-v0.1 — rhwp 에이전트 MVP

OpenAI 기반 멀티턴 에이전트로 HWP/HWPX 문서를 웹에서 편집하는 기능의 MVP. self-hosted (Docker compose), NestJS 백엔드 + rhwp-studio frontend. 본가 절차(하이퍼-워터폴) + fork 절차 다듬기 누적([methodology_refinements.md](../manual/methodology_refinements.md)).

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| [#1](https://github.com/Alpha-ChangukChoi/rhwp/issues/1) | NestJS agent-server 스켈레톤 + docker-compose 통합 | 완료 | 보고서 [task_agent-v0.1_1_report.md](../report/task_agent-v0.1_1_report.md) |
| [#2](https://github.com/Alpha-ChangukChoi/rhwp/issues/2) | OpenAI Chat Completions 클라이언트 + 환경변수 검증 | 완료 | 보고서 [task_agent-v0.1_2_report.md](../report/task_agent-v0.1_2_report.md) |
| [#3](https://github.com/Alpha-ChangukChoi/rhwp/issues/3) | hwpctl Action ↔ OpenAI tool 매핑 + 도구 호출 루프 | 완료 | 보고서 [task_agent-v0.1_3_report.md](../report/task_agent-v0.1_3_report.md) |
| [#4](https://github.com/Alpha-ChangukChoi/rhwp/issues/4) | 멀티턴 세션 히스토리 관리 (in-memory) | 완료 | 보고서 [task_agent-v0.1_4_report.md](../report/task_agent-v0.1_4_report.md) |
| [#5](https://github.com/Alpha-ChangukChoi/rhwp/issues/5) | rhwp-studio 우측 사이드바 채팅 UI | 완료 (오늘) | Stage 0 (사전 점검) → Stage 1 (agent-client + agent-store + 격리 5건) → Stage 2 (UI 컴포넌트 4종 + agent.css + 격리 6건) → Stage 3 (a) (agent/index.ts + 통합 e2e 4건) → Stage 3 (b) (main.ts 1~2줄 단독 commit + 자동 mount e2e). 본가 무수정 정책 첫 변형 (옵션 2 변형: `rhwp-studio/src/agent/` + main.ts 분리 commit). **R-014/R-015 두 번째 의무 적용 — 결정 변경 0건 + 누락 0건 유지**. R-5-K 신설 (단위 테스트 puppeteer-only, 수행계획서 (a) self-check 발견). **R-013 frontend 변형 첫 사례** — layer 1·2 모두 puppeteer 단일 인프라. 누적 e2e 16건 (15 pass + 1 skip 환경 deviation). Deviation 6건 (해결 4 + 구조 1 + 환경 1). 최종 보고서 [task_agent-v0.1_5_report.md](../report/task_agent-v0.1_5_report.md) |

## 다음 후보 (agent-v0.1 마일스톤 후속 이슈)

| 후보 | 내용 |
|------|------|
| #6 (예정) | agent-server HTTP endpoint + 인증/CORS — Stage 0 발견 §2.1 (agent-server 이미 HTTP 모드 부팅, ChatController 만 부재) 로 작업 범위 축소 |

agent-v0.1 마일스톤 진행률: **5/6 closed (83%)**. #6 등록 + 종결 시 100%.

## 별도 정리 후보 (방법론 다듬기 후속)

| 후보 | 내용 | 발생 task |
|------|------|---------|
| (R-012) | jest manual mock dup 정리 (옵션 A/B/C 중 채택) | #2 |
| (R-016 / R-013 보강) | frontend 검증 사다리 정식화 — backend (jest+compose) / frontend (puppeteer 단일 + mount 범위) 분기 | #5 |
| ~~환경 정비~~ ✅ 완료 | `pkg/` WASM 빌드 — `local/env-pkg-wasm` 브랜치 (2026-05-01) | #5 (D-5-6 해소) |
| e2e helpers.mjs 갱신 | page.click flaky 우회 (element.click()) + 한국어 type 우회 (page.evaluate value 설정) + CORS preflight 헬퍼 | #5 (D-5-2/3, D-5-1) |

## 환경 정비 후속 검증 (`local/env-pkg-wasm`, 오늘)

#5 의 deferred 검증 (D-5-6) 해소:

| 항목 | 결과 |
|------|------|
| Docker WASM 빌드 (`docker compose --env-file .env.docker run --rm wasm`) | ✅ 성공 (2분 47초) |
| `.env.docker` 작성 | UID=501 (호스트), GID=1000 (Linux 컨테이너 시스템 그룹 충돌 회피 — macOS staff GID 20 사용 시 Dockerfile chown fail 부수 발견) |
| `pkg/` 산출 (rhwp.js 228KB, rhwp_bg.wasm 4.1MB) | 호스트 owner 정합 |
| TypeScript strict (rhwp-studio 전체) | ✅ 0 에러 (기존 `@wasm/rhwp.js` 에러 2건 해소) |
| `npm run build` (production) | ✅ 통과 (689KB main + 4.1MB WASM + PWA SW) |
| 통합 e2e 5건 | ✅ **5 pass** (이전 4 pass + 1 skip → 1 skip 해제 + 실 검증) |
| 1번째 e2e | main.ts 자동 mount + 수동 mount 양립 *idempotent 검증* 으로 갱신 |

**남은 deferred** (마일스톤 회고 또는 #6): PWA SW 실측 검증 (vite preview + SW 인터셉트).

## 방법론 평가 (오늘 누적)

### R-014/R-015 두 번째 의무 적용 효과 (5 task 누적)

| 가설 | #4 (첫 의무) | #5 (두 번째 의무) | 평가 |
|------|------------|------------------|------|
| 결정 변경 건수 | 0건 (9건 결정 中) | **0건** (11건 결정 中) | **두 task 연속 0** — 가설 강화 |
| 누락 건수 | 0건 | 0건 | 유지 |
| 추천 강화 효과 | 측정 안 됨 | **R-5-B EventTarget 강화** (Stage 2 시점 *2 컴포넌트 동시 구독* 데이터 활용) | 신규 발견 |
| Manual 검증 | 0회 | 0회 | 유지 |
| 재작업 | 0회 | 0회 | 유지 |

### R-013 frontend 변형 — 첫 사례

본 task 가 R-013 의 *frontend 첫 적용*. layer 1 (격리 mount) + layer 2 (통합 mount) 모두 puppeteer 단일 인프라 + mount 범위 차별화. backend 의 *jest+compose 2 도구* 와 다른 *1 도구 + 범위 다층화* 패턴.

본가 무수정 정책의 *분리 commit* 정신과 *production 번들 검증 (R-013 layer 2 정신)* 의 commit 순서 충돌 발견 → Stage 분리 (a Vite dev / b production deferred) 로 절충. **R-016 (또는 R-013 보강) 정식화 후보** 자료 누적.

### 본가 무수정 정책 첫 변형 — 안정 운용

옵션 2 변형 (`rhwp-studio/src/agent/` 신규 폴더 + `main.ts` 1~2줄 분리 commit) 첫 적용 → 위반 0 (예외 1 = main.ts 분리 commit 명시). 메뉴 hook 의 *위임 dispatcher 우회 패턴* (`data-cmd` 부재 `.md-item` + 별도 click listener) 도 본가 무수정 정책 정신 충족.

### R-001 (이슈 등록 미니사이클) 보강 효과 — 수행계획서 (a) self-check

#5 의 R-5-K (단위 테스트 puppeteer-only) 가 *수행계획서 (a) 작성 단계* 에서 발견 — 이슈 등록 단계에서 미발견. 즉 R-001 의 (a) 단계가 *결정 항목 누락* 도 검출하는 효과 입증.

## 본 fork 의 첫 frontend task 종결 — 트렌드 종합

| 영역 | #1~#4 (backend) | #5 (frontend) | 트렌드 |
|------|---------------|--------------|------|
| 신규 의존성 | 표준 라이브러리 위주 (0~2개) | **0개** | 유지 |
| TypeScript strict | NestJS 표준 | **agent/* 전체 strict 통과** | 신규 영역 동일 정합 |
| 검증 도구 | jest + compose | **puppeteer 단일 + mount 범위** | 영역 별 분기 |
| 본가 코드 수정 | 0 | **3줄 (main.ts 분리 commit)** | 본가 정책 첫 변형 |
| Manual 검증 | 0회 | 0회 | 유지 |

본 fork 의 *backend 골격 + frontend 골격* 양 영역에서 *방법론 누적 효과* 정량 입증.
